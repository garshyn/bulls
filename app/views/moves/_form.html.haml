- move ||= @move
= form_for move do |f|
  = f.hidden_field :play_id
  .grid-x
    .cell.large-6= f.number_field :request, autofocus: true, autocomplete: :off, class: 'text-right', style: 'font-size: 28px; border: none; box-shadow: none'
    -# .cell.large-6= f.submit 'Save', class: 'expanded button'
